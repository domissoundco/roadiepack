# Tally

Minimal-weight packing calculator with three modes (Corporate, Rock & Roll, Holiday) and live weather adjustments via Open-Meteo.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Deploy

Push to GitHub, import into Vercel. No env vars needed.
